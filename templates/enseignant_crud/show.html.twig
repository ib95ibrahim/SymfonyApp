{% extends 'base.html.twig' %}

{% block title %}Enseignant{% endblock %}

{% block body %}
    <h1>Enseignant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseignant.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ enseignant.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ enseignant.lastName }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ enseignant.age }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enseignant_crud_index') }}">back to list</a>

    <a href="{{ path('app_enseignant_crud_edit', {'id': enseignant.id}) }}">edit</a>

    {{ include('enseignant_crud/_delete_form.html.twig') }}
{% endblock %}
